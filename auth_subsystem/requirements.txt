# Основные зависимости для подсистемы аутентификации
pywin32>=227; sys_platform == "win32"

# Зависимости для логирования
# (используем стандартную библиотеку logging, но для расширенного функционала)
# watchdog>=2.1.0  # для мониторинга изменений в файлах логов (опционально)